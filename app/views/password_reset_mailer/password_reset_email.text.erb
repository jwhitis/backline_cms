Someone (probably you) has requested to reset your password on the "<%= @site.title %>" website. To get started, copy and paste the URL below into your address bar.

<%= edit_password_reset_url(@user.perishable_token) %>

If you didn't make this request, you don't need to do anything. Your account is still secure. If at any point you do want to reset your password, you can do so by visiting the URL above.