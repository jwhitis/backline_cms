<div id="albums" class="max-width-sm">
  <div id="album-carousel" class="carousel slide" data-interval="false">
    <div class="carousel-inner" role="listbox">
      <% @albums.each_with_index do |album, index| %>
        <div class="item <%= "active" if index.zero? %>">
          <%= render album %>
        </div>
      <% end %>
    </div>
  </div>

  <div class="pagination-links clearfix">
    <% if @albums.count > 1 %>
      <%= link_to "#album-carousel", data: { slide: "prev" }, class: "btn-main" do %>
        <%= fa_icon("caret-left") %> PREV
      <% end %>
      <%= link_to "#album-carousel", data: { slide: "next" }, class: "btn-main" do %>
        NEXT <%= fa_icon("caret-right") %>
      <% end %>
    <% end %>
  </div>
</div>

<%= render "shared/inline_player" %>