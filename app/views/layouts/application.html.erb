<!DOCTYPE html>
<html>
<head>
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Benchmarks</title>
  <link href="http://fonts.googleapis.com/css?family=Open+Sans:400italic,400,300,700|Oswald"
    rel="stylesheet" type="text/css" />
  <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
  <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
  <%= csrf_meta_tags %>
  <%= content_for :head %>
</head>
<body>
  <div id="wrapper">
    <%= render "layouts/nav" %>

    <%= link_to root_path, remote: true, id: "banner" do %>
      <%= image_tag "http://toddfarrell-production.s3.amazonaws.com/images/banner_website.jpg" %>
    <% end %>

    <div id="main">
      <div class="container">
        <div id="content">
          <%= content_for?(:content) ? content_for(:content) : yield %>
        </div>
      </div>
    </div>

    <%= render "layouts/footer" %>
  </div>

  <%= render "layouts/flash_modal" %>
</body>
</html>