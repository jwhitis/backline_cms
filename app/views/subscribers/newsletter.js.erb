var $flashModal = $("div#flash-modal");
var newModal = "<%=j render 'layouts/flash_modal' %>";

if ($flashModal.length) {
  $flashModal.replaceWith(newModal);
} else {
  $("body").append(newModal);
}

$("div#flash-modal").modal("show");
$("div#newsletter-signup form").trigger("reset");