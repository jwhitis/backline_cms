<h2>Shows</h2>
<%= render "layouts/alerts" %>
<table class="table table-condensed admin">
  <thead>
    <tr>
      <th class="date">Date</th>
      <th>Venue</th>
      <th>Cover</th>
      <th>Address</th>
      <th class="edit"></th>
      <th class="delete"></th>
    </tr>
  </thead>
  <tbody>
    <% @shows.each do |show| %>
      <tr>
        <td class="date"><%= show.pretty_date %></td>
        <td><%= show.venue %></td>
        <td><%= show.cover %></td>
        <td><%== show.pretty_address %></td>
        <td class="edit"><%= link_to "Edit", edit_admin_show_path(show) %></td>
        <td class="delete"><%= link_to "Delete", admin_show_path(show), method: :delete,
          data: {confirm: "Are you sure you want to delete this show?"} %></td>
      </tr>
    <% end %>
  </tbody>
</table>
<%= link_to "Add Show", new_admin_show_path, class: "btn btn-primary" %>